# 一键部署到 Railway

[![Deploy on Railway](https://railway.app/button.svg)](https://railway.app/template/new?template=https://github.com/miaomiao123456/option-tracker)

## 快速部署步骤

1. **点击上面的 "Deploy on Railway" 按钮**

2. **用 GitHub 账号登录 Railway**

3. **配置环境变量**（在部署页面添加）：
   ```
   UQER_TOKEN=190bbd239ab55cb2f3f2919601622b0b793e94c35d7967e0c4b325682eddd981
   DATABASE_URL=sqlite:///./option_tracker.db
   PORT=8000
   ```

4. **点击 Deploy** - 等待 2-3 分钟

5. **获取访问地址** - Railway 会自动分配域名，类似：
   ```
   https://option-tracker-production-xxxx.up.railway.app
   ```

6. **访问系统**：
   - 首页: `https://your-domain/`
   - API文档: `https://your-domain/docs`
   - 前端: `https://your-domain/frontend`

---

## 或者更简单的方式 - Render 一键部署

1. 访问：https://render.com
2. 用 GitHub 登录
3. 点击 "New +" → "Web Service"
4. 选择仓库：`miaomiao123456/option-tracker`
5. 填写：
   - Build Command: `pip install -r requirements.txt`
   - Start Command: `uvicorn main:app --host 0.0.0.0 --port $PORT`
6. 添加环境变量（同上）
7. 点击 "Create Web Service"

完成！Render 会给您一个域名。

---

## 临时快速预览 - 使用 ngrok (本地运行)

如果只是临时几个小时展示，可以在本地运行 + ngrok 穿透：

```bash
# 在本地运行
cd /Users/pm/Documents/期权交易策略/option_tracker
uvicorn main:app --host 0.0.0.0 --port 8000

# 另开一个终端，用 ngrok 穿透
ngrok http 8000
```

ngrok 会给您一个临时外网地址，有效期几个小时。

---

**推荐**: Railway 或 Render 都是点几下就完成，最简单！
