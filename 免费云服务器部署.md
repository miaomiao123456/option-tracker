# 免费云服务器部署方案

## 🆓 免费云服务器推荐（2025年）

### 1. ⭐ Oracle Cloud（推荐）

**永久免费套餐：**
- 2个AMD VM（1 CPU + 1GB RAM）
- 4个ARM VM（4 CPU + 24GB RAM）
- 200GB 块存储
- 10TB/月 出站流量

**优点：**
- 完全免费，无需信用卡
- 配置够用（4核24G ARM性能强）
- 公网IP + 固定IP
- 适合运行爬虫和API服务

**注册地址：** https://www.oracle.com/cloud/free/

**部署步骤：**
```bash
# 1. 创建实例（选择Ubuntu 22.04 LTS ARM）
# 2. SSH连接
ssh -i your-key.pem ubuntu@your-ip

# 3. 安装Python和依赖
sudo apt update
sudo apt install -y python3 python3-pip
pip3 install -r requirements.txt

# 4. 安装Playwright
playwright install-deps
playwright install chromium

# 5. 使用systemd管理服务（见下方配置）
```

---

### 2. Google Cloud Platform (GCP)

**免费套餐：**
- e2-micro实例（0.25 CPU + 1GB RAM）
- 30GB HDD
- 1GB 出站流量/月
- $300美元试用额度（90天）

**注册地址：** https://cloud.google.com/free

---

### 3. Render.com（推荐用于API部署）

**免费套餐：**
- 750小时/月免费运行时间
- 自动HTTPS
- 持续部署（连接GitHub自动部署）

**优点：**
- 最简单！无需配置服务器
- 支持Python Web服务
- 自带域名和SSL证书

**部署步骤：**
1. 将代码推送到GitHub
2. 在 Render.com 创建 Web Service
3. 连接GitHub仓库
4. 设置启动命令：`uvicorn main:app --host 0.0.0.0 --port $PORT`
5. 自动部署完成！

**注册地址：** https://render.com/

---

### 4. Railway.app

**免费套餐：**
- $5/月免费额度
- 512MB RAM
- 支持Python/FastAPI

**注册地址：** https://railway.app/

---

## 🚀 推荐部署方案

### 方案A：Oracle Cloud（完整功能）

**适用场景：** 需要运行爬虫 + API + 数据库

```bash
# Oracle Cloud ARM实例部署

# 1. 克隆代码
git clone <your-repo>
cd option_tracker

# 2. 安装依赖
pip3 install -r requirements.txt
playwright install chromium

# 3. 配置环境变量
cp .env.example .env
vim .env  # 填入账号密码

# 4. 创建systemd服务
sudo vim /etc/systemd/system/option-tracker.service
```

**systemd服务配置：**
```ini
[Unit]
Description=OptionAlpha API Service
After=network.target

[Service]
Type=simple
User=ubuntu
WorkingDirectory=/home/ubuntu/option_tracker
Environment="PATH=/home/ubuntu/.local/bin:/usr/local/bin:/usr/bin"
ExecStart=/usr/bin/python3 -m uvicorn main:app --host 0.0.0.0 --port 8000
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

**启动服务：**
```bash
sudo systemctl daemon-reload
sudo systemctl enable option-tracker
sudo systemctl start option-tracker
sudo systemctl status option-tracker
```

**配置Nginx反向代理：**
```bash
sudo apt install nginx

sudo vim /etc/nginx/sites-available/option-tracker
```

```nginx
server {
    listen 80;
    server_name your-domain.com;

    location / {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    location /ws {
        proxy_pass http://127.0.0.1:8000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}
```

```bash
sudo ln -s /etc/nginx/sites-available/option-tracker /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl restart nginx
```

**开放端口：**
```bash
# Oracle Cloud需要在防火墙规则中开放80和8000端口
sudo iptables -I INPUT -p tcp --dport 80 -j ACCEPT
sudo iptables -I INPUT -p tcp --dport 8000 -j ACCEPT
sudo iptables-save
```

---

### 方案B：Render.com（仅API，最简单）

**适用场景：** 只部署API，爬虫本地运行

**步骤：**

1. **推送代码到GitHub**
```bash
cd option_tracker
git init
git add .
git commit -m "Initial commit"
git remote add origin <your-github-repo>
git push -u origin main
```

2. **创建render.yaml配置文件**
```yaml
# render.yaml
services:
  - type: web
    name: option-alpha-api
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn main:app --host 0.0.0.0 --port $PORT"
    envVars:
      - key: DATABASE_URL
        value: sqlite:///./option_tracker.db
      - key: JYK_USER
        value: 18321399574
      - key: JYK_PASS
        sync: false  # 敏感信息通过Dashboard设置
      - key: GEMINI_API_KEY
        sync: false
```

3. **在Render.com创建服务**
   - 登录 https://render.com/
   - New → Web Service
   - Connect GitHub仓库
   - 自动识别Python项目
   - 点击Deploy

4. **设置环境变量**
   - 在Dashboard → Environment中添加敏感变量
   - `JYK_PASS`, `GEMINI_API_KEY`

5. **获取API地址**
   - Render会提供：`https://your-app.onrender.com`
   - 修改前端`API_BASE`为此地址

**注意：** Render免费版会休眠，首次请求需15秒启动

---

### 方案C：混合部署（最优方案）

- **API**: 部署到 Render.com（免费+HTTPS）
- **爬虫**: 本地Mac运行（定时任务）
- **前端**: 部署到 Netlify/Vercel（免费）

**优点：**
- 完全免费
- API自带HTTPS
- 前端CDN加速
- 爬虫本地运行更稳定

---

## 🌐 免费前端托管

### Netlify（推荐）

**免费套餐：**
- 100GB/月流量
- 自动HTTPS
- 持续部署
- 自定义域名

**部署步骤：**
```bash
# 1. 安装Netlify CLI
npm install -g netlify-cli

# 2. 部署前端
cd option_tracker
netlify deploy --dir=. --prod

# 3. 选择frontend.html为入口文件
```

**或者使用Web界面：**
1. 登录 https://www.netlify.com/
2. New Site → 拖拽 frontend.html
3. 自动部署完成

---

## 💾 免费数据库

### 1. Supabase（推荐）

**免费套餐：**
- 500MB PostgreSQL数据库
- 1GB文件存储
- 50MB数据库带宽/月

**注册：** https://supabase.com/

### 2. PlanetScale

**免费套餐：**
- 5GB存储
- 1亿行读取/月

**注册：** https://planetscale.com/

---

## 📊 完整免费部署架构（推荐）

```
用户浏览器
    ↓
Netlify (前端)
    ↓
Render.com (API)
    ↓
Supabase (PostgreSQL)
    ↑
本地Mac (爬虫定时任务)
```

**成本：** $0/月
**性能：** 满足个人使用
**稳定性：** 高（多服务商分散风险）

---

## 🔧 快速部署命令集合

### Oracle Cloud部署
```bash
# 一键安装脚本
curl -fsSL https://raw.githubusercontent.com/yourusername/option-tracker/main/deploy.sh | bash
```

### Render.com部署
```bash
# 推送代码
git push origin main
# 自动部署完成！
```

### 前端部署到Netlify
```bash
netlify deploy --prod
```

---

## ⚠️ 注意事项

1. **Oracle Cloud注册**
   - 需要信用卡验证（但不会扣费）
   - 选择韩国/日本数据中心延迟低

2. **Playwright在云服务器**
   ```bash
   # 可能需要额外依赖
   playwright install-deps chromium
   ```

3. **数据库迁移到PostgreSQL**
   ```python
   # 修改.env
   DATABASE_URL=postgresql://user:pass@host:5432/dbname
   ```

4. **爬虫反爬虫**
   - 云服务器IP可能被封
   - 建议本地运行爬虫，API部署云端

---

## 🎯 推荐方案总结

| 需求 | 推荐方案 | 成本 |
|------|---------|------|
| 个人学习 | Render.com (API) + 本地爬虫 | $0 |
| 完整功能 | Oracle Cloud (全部) | $0 |
| 生产环境 | Oracle Cloud + Supabase | $0 |
| 极简部署 | Render.com 一键部署 | $0 |

**我的建议：** 使用 **Render.com部署API + 本地爬虫 + Netlify前端**

这样既简单，又完全免费，还有HTTPS和CDN加速！

---

需要我帮你配置哪个方案吗？我可以直接帮你生成部署配置文件！
