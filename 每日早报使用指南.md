# 📰 每日早报功能使用指南

## 📅 功能说明

每日早报功能会自动整合系统中的所有数据源,包括:
- 📈 市场重要资讯 (金十数据)
- 🎯 顶级交易机会 (S/A级机会)
- 📊 研报机构观点
- 💰 资金异动品种
- ⚠️ 风险预警信息
- 📉 技术面信号

## 🕐 自动生成时间

**每天早上 8:30** 自动生成当日早报

## 🌐 访问方式

### 方式1: 主页面访问
1. 访问 http://localhost:8001/frontend.html
2. 点击顶部 "🌅 每日早报" 标签

### 方式2: 独立页面访问
直接访问 http://localhost:8001/daily_report.html

## 🔧 API接口

### 1. 获取最新早报
```
GET /api/v1/daily-report/latest
```

返回示例:
```json
{
  "success": true,
  "report_date": "2025-12-28",
  "generate_time": "2025-12-28 08:30:15",
  "executive_summary": "今日要点摘要...",
  "content": {
    "market_news": [...],
    "top_opportunities": [...],
    ...
  }
}
```

### 2. 按日期获取早报
```
GET /api/v1/daily-report/by-date?date=2025-12-27
```

### 3. 获取早报列表
```
GET /api/v1/daily-report/list?days=7
```

### 4. 手动生成早报
```
POST /api/v1/daily-report/generate
```

### 5. 预览早报 (不保存)
```
GET /api/v1/daily-report/preview
```

## 📊 早报包含内容

### 1. 执行摘要
- 今日重要资讯提示
- S/A级机会品种
- 机构一致性观点
- 资金流向异动
- 风险预警

### 2. 顶级交易机会
- **S级机会**: 顶级交易机会,综合得分最高
- **A级机会**: 优质交易机会,值得关注
- 包含: 品种代码、方向、置信度、交易理由

### 3. 市场资讯
- 从金十数据抓取最新快讯
- 标注重要程度(高/中/低)
- 自动筛选期货相关资讯

### 4. 研报要点
- **强烈看多**: 80%以上机构看多的品种
- **强烈看空**: 80%以上机构看空的品种
- **分歧较大**: 40-60%观点分歧品种

### 5. 资金流向
- **Top净流入**: 近3天资金大幅流入品种
- **Top净流出**: 近3天资金大幅流出品种

### 6. 风险预警
- 虚实比异常 (>100)
- 逼仓风险品种

## 🚀 使用示例

### 命令行测试
```bash
# 切换到项目目录
cd /Users/pm/Documents/期权交易策略/option_tracker

# 立即生成早报
python -c "
import asyncio
from app.services.daily_report_generator import DailyReportGenerator

async def test():
    generator = DailyReportGenerator()
    report = await generator.generate_daily_report()
    print('早报生成成功!')
    print(f'报告日期: {report[\"report_date\"]}')
    print(f'摘要: {report[\"executive_summary\"]}')

asyncio.run(test())
"
```

### Python脚本调用
```python
import asyncio
from app.services.daily_report_generator import DailyReportGenerator

async def main():
    generator = DailyReportGenerator()
    report = await generator.generate_daily_report()
    return report

# 运行
report = asyncio.run(main())
print(report['executive_summary'])
```

### API调用
```bash
# 获取最新早报
curl http://localhost:8001/api/v1/daily-report/latest

# 手动生成早报
curl -X POST http://localhost:8001/api/v1/daily-report/generate
```

## ⚙️ 配置说明

### 修改生成时间
编辑 `app/services/scheduler.py`:
```python
# 修改这一行的 hour 和 minute 参数
self.scheduler.add_job(
    self.generate_morning_report,
    trigger=CronTrigger(hour=8, minute=30),  # 修改这里
    ...
)
```

### 调整数据源
编辑 `app/services/daily_report_generator.py`,在各个 `_get_*` 方法中修改数据查询逻辑

## 📝 数据要求

早报功能依赖以下数据表:
- ✅ `research_reports` - 研报数据 (已存在)
- ✅ `daily_blueprints` - 交易可查蓝图 (已存在)
- ✅ `market_full_view` - 市场全景数据 (已存在)
- ⚠️ `comprehensive_opportunities` - 综合机会表 (需运行机会雷达分析)
- ⚠️ `warehouse_receipts` - 仓单数据 (用于虚实比计算)

**说明**: 部分功能在缺少数据时会显示"暂无数据",不影响早报整体生成

## 🎯 完整工作流程

1. **每天8:30自动触发**:
   - 系统后台定时任务自动运行

2. **数据采集**:
   - 并发获取7个数据源的数据
   - 从金十数据抓取最新资讯
   - 从数据库读取各类分析结果

3. **智能分析**:
   - 评估新闻重要性
   - 筛选S/A级交易机会
   - 分析机构观点一致性
   - 识别资金流向异动

4. **生成报告**:
   - 创建执行摘要
   - 组装完整报告
   - 保存到数据库

5. **前端展示**:
   - 用户访问早报页面
   - 自动加载最新早报
   - 美观展示所有内容

## 💡 使用技巧

1. **每天开盘前查看早报**: 了解今日重点关注品种和方向
2. **关注S级机会**: 这些是系统评估的顶级交易机会
3. **结合资讯和研报**: 市场资讯+机构观点,双重验证
4. **警惕风险提示**: 虚实比异常品种需谨慎
5. **查看资金流向**: 大资金动向往往预示行情变化

## 🔍 故障排查

### 早报显示"暂无数据"
1. 检查后端服务是否运行: `curl http://localhost:8001/api/v1/daily-report/latest`
2. 查看数据采集日志: 检查是否有数据采集任务运行
3. 手动触发生成: 点击"立即生成最新早报"按钮

### 定时任务未执行
1. 检查后端日志,确认调度器已启动
2. 查看日志中是否有 "每日早报定时任务已启动" 消息
3. 手动触发测试: `curl -X POST http://localhost:8001/api/v1/daily-report/generate`

### 新闻抓取失败
- 金十数据API可能暂时不可用
- 系统会自动使用备用提示信息
- 不影响其他模块的数据展示

## 📞 技术支持

如有问题,可查看:
- 后端日志: `server.log`
- 数据库: `option_tracker.db` 中的 `daily_reports` 表
- API文档: http://localhost:8001/docs

---

**更新时间**: 2025-12-28
**版本**: 1.0
**状态**: ✅ 已上线可用
