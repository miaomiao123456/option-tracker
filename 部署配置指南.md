# 期权交易策略系统 - 部署配置指南

## 问题说明

当前系统采用**前后端分离架构**：
- **前端**：静态HTML文件（frontend.html），可部署到Netlify等静态托管服务
- **后端**：FastAPI服务（Python），需要部署到支持Python的服务器

**本地访问正常，外部访问失败**的原因：
- 本地访问时，前端和后端都在localhost，API调用正常
- 外部访问时（如通过Netlify），前端尝试调用Netlify域名的API，但后端服务不在那里

## 解决方案

### 方案1：完整部署（推荐）

**前端部署到Netlify + 后端部署到Railway**

#### 步骤1：部署后端到Railway

1. 访问 [Railway.app](https://railway.app/)
2. 创建新项目，连接GitHub仓库
3. Railway会自动检测到`railway.json`配置
4. 等待部署完成，获得后端URL（如：`https://your-app.railway.app`）

#### 步骤2：配置前端API地址

在`frontend.html`中找到以下配置（约第1895行）：

```javascript
const BACKEND_API_URL = ''; // 留空则自动检测
```

修改为你的Railway后端地址：

```javascript
const BACKEND_API_URL = 'https://your-app.railway.app';
```

#### 步骤3：部署前端到Netlify

1. 将修改后的`frontend.html`提交到仓库
2. 在Netlify中配置发布目录为`option_tracker`
3. 部署完成后，访问Netlify提供的URL即可

### 方案2：全部部署到Railway

如果不想分离部署，可以将前后端都部署到Railway：

1. 后端FastAPI已配置了静态文件服务
2. 保持`BACKEND_API_URL = ''`（留空）
3. 直接部署到Railway，访问 `https://your-app.railway.app/frontend`

### 方案3：本地测试 + 内网穿透

用于临时演示或测试：

1. 使用ngrok或frp等工具暴露本地8000端口
2. 获得公网URL（如：`https://abc123.ngrok.io`）
3. 在`frontend.html`中配置：
   ```javascript
   const BACKEND_API_URL = 'https://abc123.ngrok.io';
   ```

## 环境变量配置

### Railway部署需要配置的环境变量

在Railway项目设置中添加以下环境变量：

```bash
# Gemini API配置
GEMINI_API_KEY=你的API密钥
GEMINI_BASE_URL=https://www.apillm.online/v1

# 智汇期讯Token（从浏览器抓取）
ZHIHUI_AUTHORIZATION=Bearer xxx...

# 数据库路径（Railway会自动处理）
DATABASE_URL=sqlite:///./option_tracker.db
```

## CORS配置说明

后端已经配置了CORS支持（`main.py`第26-33行）：

```python
app.add_middleware(
    CORSMiddleware,
    allow_origins=["*"],  # 允许所有来源
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)
```

**安全建议**：生产环境应该限制具体域名，例如：

```python
allow_origins=[
    "https://your-frontend.netlify.app",
    "https://your-app.railway.app"
]
```

## 常见问题排查

### 1. API调用失败：Network Error

**原因**：后端服务未启动或URL配置错误

**解决**：
- 检查Railway部署状态
- 确认`BACKEND_API_URL`配置正确
- 打开浏览器控制台查看具体错误

### 2. 图表DOM未找到

**原因**：Vue渲染未完成时就尝试创建图表

**解决**：
- 已添加重试逻辑（500ms后重试）
- 已添加Tab切换监听，自动重新渲染

### 3. V3 API不可用，回退到V2

**原因**：V3服务运行在不同端口（8001）

**解决**：
- 确保V3服务也已部署（或在同一端口）
- 如果不使用V3，忽略此警告，系统会自动使用V2

### 4. 混合内容错误（Mixed Content）

**原因**：HTTPS页面调用HTTP API

**解决**：
- 确保后端也使用HTTPS
- Railway默认提供HTTPS支持

## 验证部署

部署完成后，访问前端URL，打开浏览器控制台：

1. **成功标志**：
   - 看到"✓ 使用V3优化数据"或"V2数据加载成功"
   - 看到图表正常渲染
   - 没有红色错误信息

2. **失败标志**：
   - "V3 API不可用,回退到V2: Failed to fetch"
   - "图表DOM未找到"
   - 大量红色错误

## 快速测试配置

修改`frontend.html`第1895行，临时使用你的Railway URL：

```javascript
const BACKEND_API_URL = 'https://your-app.railway.app';
```

然后在本地打开`frontend.html`测试是否能连接到后端。

## 推荐部署架构

```
用户浏览器
    ↓
Netlify (前端HTML)
    ↓ API调用
Railway (后端FastAPI)
    ↓ 数据库
SQLite (自动备份)
```

## 注意事项

1. **数据库**：Railway使用临时文件系统，需要配置持久化存储
2. **定时任务**：确保后端持续运行以执行定时爬虫
3. **API密钥**：不要在前端暴露敏感信息
4. **CORS配置**：生产环境限制允许的域名

## 联系支持

如遇问题，请检查：
1. Railway部署日志
2. 浏览器控制台错误
3. Network面板查看API请求详情
