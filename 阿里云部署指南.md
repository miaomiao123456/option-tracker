# 阿里云部署指南

## 服务器信息
- **服务器IP**: 120.27.159.14
- **部署目录**: /opt/option-tracker
- **访问地址**: http://120.27.159.14/frontend
- **运行端口**: 80 (HTTP标准端口)

## 部署架构

**单服务器架构**：前后端部署在同一台阿里云服务器
```
用户浏览器
    ↓ http://120.27.159.14
阿里云服务器 (120.27.159.14:80)
    ├── FastAPI后端 (Uvicorn)
    │   ├── /api/v1/*        (V1 API)
    │   ├── /api/v3/*        (V3 API)
    │   └── /api/term-structure/*
    └── /frontend            (静态HTML页面)
```

## 快速部署

### 步骤1: 在本地Mac上传文件

```bash
# 在本地终端执行
scp /tmp/deploy_fixed.tar.gz root@120.27.159.14:/tmp/
```

### 步骤2: 在服务器上部署

```bash
# SSH连接服务器后执行
pkill -f uvicorn ; \
rm -rf /opt/option-tracker/* && \
cd /opt/option-tracker && \
tar -xzf /tmp/deploy_fixed.tar.gz && \
nohup python3 -m uvicorn main:app --host 0.0.0.0 --port 80 > app.log 2>&1 & \
sleep 5 && \
curl http://localhost/health
```

**命令说明**：
- `pkill -f uvicorn` - 停止旧服务
- `rm -rf /opt/option-tracker/*` - 清空旧文件
- `tar -xzf /tmp/deploy_fixed.tar.gz` - 解压新文件
- `nohup ... &` - 后台运行服务
- `curl http://localhost/health` - 验证服务是否启动

### 步骤3: 验证部署

访问 http://120.27.159.14/frontend

**预期结果**：
- ✅ 页面正常加载
- ✅ 数据表格显示
- ✅ 图表正常渲染
- ✅ 无控制台错误

## 前端配置说明

### API URL自动检测

前端已配置自动检测环境，无需手动修改：

```javascript
// frontend.html 第1895行
const BACKEND_API_URL = ''; // 留空，自动检测
```

**自动检测逻辑**：
- 本地开发：http://localhost:8000
- 阿里云：http://120.27.159.14 (自动使用当前域名)

### API端点映射

| 前端访问 | 实际API端点 |
|---------|------------|
| http://120.27.159.14/api/v1/summary | FastAPI /api/v1/summary |
| http://120.27.159.14/api/v3/analysis | FastAPI /api/v3/analysis |
| http://120.27.159.14/api/term-structure | FastAPI /api/term-structure |

## 服务管理

### 查看服务状态

```bash
# 查看进程
ps aux | grep uvicorn

# 查看日志
tail -f /opt/option-tracker/app.log

# 查看端口占用
netstat -tulpn | grep :80
```

### 停止服务

```bash
pkill -f uvicorn
```

### 启动服务

```bash
cd /opt/option-tracker
nohup python3 -m uvicorn main:app --host 0.0.0.0 --port 80 > app.log 2>&1 &
```

### 重启服务

```bash
pkill -f uvicorn && \
cd /opt/option-tracker && \
nohup python3 -m uvicorn main:app --host 0.0.0.0 --port 80 > app.log 2>&1 &
```

## 环境要求

### Python依赖
```bash
# 确保已安装
pip3 install -r requirements.txt
```

### 主要依赖
- FastAPI >= 0.100.0
- Uvicorn >= 0.23.0
- SQLAlchemy >= 2.0.0
- Playwright (用于爬虫)
- requests, beautifulsoup4

### 端口要求
- **80端口**：HTTP服务（需要root权限）
- 确保防火墙开放80端口

## 数据库

### 位置
```
/opt/option-tracker/option_tracker.db
```

### 备份
```bash
# 手动备份
cp /opt/option-tracker/option_tracker.db \
   /opt/option-tracker/backup_$(date +%Y%m%d_%H%M%S).db

# 查看备份
ls -lh /opt/option-tracker/backup_*.db
```

## 定时任务

服务启动后会自动执行以下定时任务：

1. **智汇期讯** - 每30分钟
2. **方期看盘早盘** - 每天08:50
3. **方期看盘夜盘** - 每天20:50
4. **交易可查** - 每天19:00
5. **Openvlab** - 交易时段每分钟
6. **每日早报** - 每天08:30

## 故障排查

### 1. 服务无法启动

**检查端口占用**：
```bash
lsof -i :80
# 如果被占用，kill掉占用进程
```

**检查Python路径**：
```bash
which python3
python3 --version  # 应该 >= 3.8
```

### 2. API调用失败

**检查服务状态**：
```bash
curl http://localhost/health
# 应返回 {"status":"ok"}
```

**查看错误日志**：
```bash
tail -100 /opt/option-tracker/app.log
```

### 3. 前端无法加载

**检查frontend.html**：
```bash
ls -lh /opt/option-tracker/frontend.html
```

**测试API连接**：
打开浏览器控制台，查看Network标签

### 4. 图表不显示

**浏览器控制台检查**：
- 看是否有"图表DOM未找到"错误
- 检查ECharts库是否加载成功

**等待重试**：
- 图表渲染有500ms重试机制
- 切换Tab会自动重新渲染

### 5. 数据未更新

**手动触发爬虫**：
```bash
cd /opt/option-tracker
python3 -c "from app.crawlers.zhihui_spider import ZhihuiSpider; ZhihuiSpider().run()"
```

**检查定时任务**：
```bash
tail -50 /opt/option-tracker/app.log | grep "定时任务"
```

## 性能优化

### 1. 使用Nginx反向代理（可选）

```nginx
server {
    listen 80;
    server_name 120.27.159.14;

    location / {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

### 2. 增加Uvicorn workers

```bash
# 多进程模式
nohup python3 -m uvicorn main:app \
    --host 0.0.0.0 \
    --port 80 \
    --workers 4 \
    > app.log 2>&1 &
```

### 3. 数据库优化

```bash
# 定期清理旧数据
sqlite3 option_tracker.db "DELETE FROM option_flows WHERE created_at < date('now', '-30 days');"
sqlite3 option_tracker.db "VACUUM;"
```

## 监控脚本

创建监控脚本 `/opt/monitor.sh`：

```bash
#!/bin/bash
# 检查服务是否运行
if ! pgrep -f "uvicorn main:app" > /dev/null; then
    echo "[$(date)] Service down, restarting..."
    cd /opt/option-tracker
    nohup python3 -m uvicorn main:app --host 0.0.0.0 --port 80 > app.log 2>&1 &
fi
```

添加到crontab：
```bash
# 每5分钟检查一次
*/5 * * * * /opt/monitor.sh >> /var/log/option-monitor.log 2>&1
```

## 安全建议

1. **限制CORS域名**：
   修改 `main.py` 第29行：
   ```python
   allow_origins=["http://120.27.159.14"]
   ```

2. **配置防火墙**：
   ```bash
   # 只开放必要端口
   ufw allow 80/tcp
   ufw allow 22/tcp
   ufw enable
   ```

3. **定期更新**：
   ```bash
   pip3 install --upgrade -r requirements.txt
   ```

## 常用命令速查

```bash
# 查看服务状态
systemctl status uvicorn  # 如果配置了systemd

# 查看实时日志
tail -f /opt/option-tracker/app.log

# 快速重启
pkill -f uvicorn && cd /opt/option-tracker && nohup python3 -m uvicorn main:app --host 0.0.0.0 --port 80 > app.log 2>&1 &

# 测试API
curl http://localhost/health
curl http://localhost/api/v1/summary/top-movers

# 查看数据库
sqlite3 /opt/option-tracker/option_tracker.db "SELECT count(*) FROM commodities;"
```

## 更新流程

每次更新代码后：

1. 本地打包上传
2. 服务器解压部署
3. 重启服务
4. 验证功能

完整命令：
```bash
# 本地
scp /tmp/deploy_fixed.tar.gz root@120.27.159.14:/tmp/

# 服务器
pkill -f uvicorn && \
rm -rf /opt/option-tracker/* && \
cd /opt/option-tracker && \
tar -xzf /tmp/deploy_fixed.tar.gz && \
nohup python3 -m uvicorn main:app --host 0.0.0.0 --port 80 > app.log 2>&1 & \
sleep 5 && \
curl http://localhost/health && \
echo "部署完成，访问 http://120.27.159.14/frontend"
```

## 支持

如遇问题，请检查：
1. `/opt/option-tracker/app.log` - 应用日志
2. 浏览器控制台 - 前端错误
3. Network面板 - API请求状态
