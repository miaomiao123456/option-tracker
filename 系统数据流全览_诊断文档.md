# 系统数据流全览 - 诊断文档

生成时间: 2026-01-12

---

## 📊 页面数据流分析

### 1. 总览页面 (overview)

#### 数据来源

| 数据项 | 函数 | API端点 | 数据库表 | 更新频率 | 状态 |
|-------|------|---------|---------|---------|------|
| **品种分析列表** | `loadV2Data()` | `/api/v1/analysis/v2/overview` | ❌ CSV文件 | ⚠️ 手动更新 | **问题** |
| **研报列** | 同上 | 同上 | `期权分析_总览_V2.csv` | ⚠️ 手动更新 | **问题** |
| **期限结构列** | 同上 | 同上 | 同上 | ⚠️ 手动更新 | **问题** |
| **智汇期讯情绪** | `fetchZhihuiData()` | `/api/v1/fundamental/zhihui/market-sentiment` | `market_full_view` | ✅ 每30分钟 | 正常 |
| **机构席位** | `loadDivergentFromServer()` | `/api/v1/capital/divergent-scan/latest` | `divergent_scan_result.json` | ✅ 每天18:00 | 正常 |
| **虚实比** | `loadVRData()` | `/api/v1/option/virtual-real-ratio/all` | `option_pcr_data` | ✅ 每天18:00 | 正常 |

**⚠️ 关键问题**:
- CSV文件 `期权分析_总览_V2.csv` 没有定时任务自动生成
- "研报"列和"期限结构"列的数据可能过时
- CSV数据与数据库数据不同步

**API调用代码** (frontend.html:2687-2748):
```javascript
const loadV2Data = async () => {
    v2Loading.value = true;
    try {
        const apiBase = getApiBase();
        const response = await fetch(`${apiBase}/analysis/v2/overview`);

        if (!response.ok) {
            throw new Error(`HTTP ${response.status}`);
        }

        const result = await response.json();

        if (result.success) {
            v2Data.value = result.data;  // 来自CSV文件!
            v2Stats.value = result.stats;
        }
    } catch (error) {
        console.error('加载V2数据失败:', error);
        ElMessage.error('加载数据失败');
    } finally {
        v2Loading.value = false;
    }
};
```

**后端代码** (app/routers/analysis_v2.py:22-67):
```python
@router.get("/overview")
async def get_analysis_overview(db: Session = Depends(get_db)):
    csv_path = DATA_DIR / "期权分析_总览_V2.csv"  # 静态CSV文件!

    if not csv_path.exists():
        # 返回空数据
        pass

    df = pd.read_csv(csv_path)  # 直接读取CSV
    return {"success": True, "data": df.to_dict('records')}
```

---

### 2. 基本面页面 (fundamental)

#### 数据来源

| 数据项 | 函数 | API端点 | 数据库表 | 更新频率 | 状态 |
|-------|------|---------|---------|---------|------|
| **智汇期讯统计** | `fetchZhihuiStats()` | `/api/v1/fundamental/zhihui/sentiment-stats` | `market_full_view` | ✅ 每30分钟 | 正常 |
| **综合机会统计** | `loadComprehensiveStats()` | `/api/v1/analysis/v2/comprehensive-stats` | ⚠️ CSV文件 | ⚠️ 手动更新 | **问题** |
| **机会列表** | `loadOpportunities()` | `/api/v1/analysis/v2/opportunities` | ⚠️ CSV文件 | ⚠️ 手动更新 | **问题** |

**API调用代码** (frontend.html:2152-2183):
```javascript
const fetchZhihuiStats = async () => {
    zhihuiStatsLoading.value = true;
    try {
        const apiBase = getApiBase();
        const response = await fetch(`${apiBase}/fundamental/zhihui/sentiment-stats`);

        if (response.ok) {
            const data = await response.json();
            zhihuiStats.value = data;  // 来自数据库 ✅
        }
    } catch (error) {
        console.error('加载智汇期讯统计失败:', error);
    } finally {
        zhihuiStatsLoading.value = false;
    }
};
```

---

### 3. 持仓分析页面 (capital)

#### 数据来源

| 数据项 | 函数 | API端点 | 数据库表 | 更新频率 | 状态 |
|-------|------|---------|---------|---------|------|
| **期权资金流** | `fetchCapitalPositions()` | `/api/v1/capital/option-flow/all` | `option_flows` | ✅ 交易时段每分钟 | 正常 |
| **持仓数据** | `loadPositionData()` | `/api/v1/capital/jiaoyikecha/{variety}/{contract}` | ⚠️ 实时爬取 | ⚠️ 实时 | 正常 |
| **持仓趋势** | 同上 | `/api/v1/capital/jiaoyikecha/{variety}/{contract}/full` | ⚠️ 实时爬取 | ⚠️ 实时 | 正常 |
| **价格季节性** | `loadSeasonalPriceData()` | `/api/v1/capital/seasonal-price` | ⚠️ akshare实时 | ⚠️ 实时 | 正常 |

**API调用代码** (frontend.html:3984-4041):
```javascript
const loadPositionData = async () => {
    positionLoading.value = true;
    try {
        // 📍 关键: 使用positionVarietyName和positionContractCode
        const posUrl = `${API_BASE}/capital/jiaoyikecha/${encodeURIComponent(positionVarietyName.value)}/${positionContractCode.value}`;
        const posResponse = await fetch(posUrl);  // ✅ 正确传递参数

        const posResult = await posResponse.json();

        if (posResult.success) {
            positionData.value = posResult;  // ✅ 实时数据
        }

        // 加载完整数据(包含趋势)
        const fullUrl = `${API_BASE}/capital/jiaoyikecha/${encodeURIComponent(positionVarietyName.value)}/${positionContractCode.value}/full`;
        const fullResponse = await fetch(fullUrl);  // ✅ 正确传递参数

        const fullResult = await fullResponse.json();

        if (fullResult.success) {
            positionFullData.value = fullResult.data;  // ✅ 实时趋势数据

            // 渲染图表
            await Vue.nextTick();
            setTimeout(() => {
                renderPositionCharts();  // ✅ 使用positionFullData渲染
            }, 200);
        }
    } catch (err) {
        console.error('加载失败:', err);
    } finally {
        positionLoading.value = false;
    }
};
```

**后端代码** (app/routers/capital.py:389-448):
```python
@router.get("/jiaoyikecha/{variety_name}/{contract_code}")
async def get_jiaoyikecha_positions(
        variety_name: str,  # ✅ 正确接收参数
        contract_code: str,  # ✅ 正确接收参数
        target_date: Optional[date] = None
):
    spider = JiaoyikechaSpider()

    # ✅ 正确传递参数给爬虫
    result = spider.fetch_position_data(
        variety=variety_name,
        contract_code=contract_code,
        query_date=query_date
    )

    return {
        "success": True,
        "variety": variety_name,  # ✅ 返回正确的品种
        "contract_code": contract_code,  # ✅ 返回正确的合约
        "long_positions": result['long_positions'],
        "short_positions": result['short_positions']
    }
```

**爬虫代码** (app/crawlers/jiaoyikecha_spider.py:136-199):
```python
def fetch_position_data(
    self,
    variety: str,  # ✅ 正确接收参数
    contract_code: str,  # ✅ 正确接收参数
    query_date: Optional[date] = None
) -> Dict:
    url = f"{self.base_url}/ajax/variety_position.php?v=f0799170"
    data = {
        'variety': variety,  # ✅ 正确发送到交易可查API
        'code': contract_code,  # ✅ 正确发送到交易可查API
        'date': date_str
    }

    logger.info(f"正在获取 {variety}({contract_code}) {date_str} 的席位数据...")

    response = self.session.post(url, data=data, timeout=10)
    result = response.json()

    parsed_data = self._parse_position_data(result, variety, contract_code, date_str)
    return parsed_data
```

**🔍 诊断结论**:
- 前端正确传递 `positionVarietyName.value` 和 `positionContractCode.value` ✅
- API路由正确接收 `variety_name` 和 `contract_code` ✅
- 爬虫正确使用参数调用交易可查API ✅
- 数据解析正确返回品种和合约信息 ✅

**❓ 用户问题: "切换不同席位出来的数据都一样"**

**可能原因**:
1. **交易可查网站缓存** - 网站本身可能缓存了数据
2. **Cookie/Session问题** - 预设Cookie可能导致某些品种无权限或返回默认数据
3. **前端浏览器缓存** - 浏览器可能缓存了API响应
4. **数据解析问题** - 某些品种的数据格式不同，解析失败返回默认值

**建议排查**:
```javascript
// 在loadPositionData函数开头添加调试日志
console.log('正在加载持仓数据:');
console.log('  品种名称:', positionVarietyName.value);
console.log('  合约代码:', positionContractCode.value);
console.log('  API URL:', posUrl);

// 在收到响应后添加
console.log('API响应:', posResult);
console.log('  品种:', posResult.variety);
console.log('  合约:', posResult.contract_code);
console.log('  多头数据条数:', posResult.long_positions?.length);
console.log('  空头数据条数:', posResult.short_positions?.length);
```

---

### 4. 席位分析页面 (seat-analysis)

#### 数据来源

| 数据项 | 函数 | API端点 | 数据库表 | 更新频率 | 状态 |
|-------|------|---------|---------|---------|------|
| **机构散户对立** | `loadDivergentFromServer()` | `/api/v1/capital/divergent-scan/latest` | `divergent_scan_result.json` | ✅ 每天18:00 | 正常 |
| **席位数据** | `loadSeatAnalysisData()` | `/api/v1/capital/jiaoyikecha/{variety}/{contract}` | ⚠️ 实时爬取 | ⚠️ 实时 | 正常 |

**API调用代码** (frontend.html:4635-4685):
```javascript
const loadSeatAnalysisData = async () => {
    seatAnalysisLoading.value = true;
    try {
        const apiBase = getApiBase();

        // 📍 关键: 使用seatVarietyName和seatContractCode
        const url = `${apiBase}/capital/jiaoyikecha/${encodeURIComponent(seatVarietyName.value)}/${seatContractCode.value}`;
        const response = await fetch(url);  // ✅ 正确传递参数

        if (!response.ok) {
            throw new Error(`HTTP ${response.status}`);
        }

        const result = await response.json();

        if (result.success) {
            seatAnalysisData.value = result;  // ✅ 实时数据

            // 计算机构和散户持仓
            calculateInstitutionRetail();
        }
    } catch (error) {
        console.error('加载席位数据失败:', error);
        ElMessage.error('加载失败');
    } finally {
        seatAnalysisLoading.value = false;
    }
};
```

**🔍 诊断结论**:
- 使用与"持仓分析"相同的API端点 ✅
- 参数传递逻辑相同 ✅

**❓ 如果这个页面也有"数据都一样"的问题，原因应该相同**

---

### 5. 虚实比页面 (virtual-real-ratio)

#### 数据来源

| 数据项 | 函数 | API端点 | 数据库表 | 更新频率 | 状态 |
|-------|------|---------|---------|---------|------|
| **虚实比数据** | `loadVirtualRealRatioData()` | `/api/v1/option/virtual-real-ratio/all` | `option_pcr_data` | ✅ 每天18:00 | 正常 |

**API调用代码** (frontend.html:3173-3197):
```javascript
const loadVirtualRealRatioData = async () => {
    virtualRealRatioLoading.value = true;
    try {
        const apiBase = getApiBase();
        const response = await fetch(`${apiBase}/option/virtual-real-ratio/all`);

        if (!response.ok) {
            throw new Error(`HTTP ${response.status}`);
        }

        const result = await response.json();

        if (result.success) {
            virtualRealRatioData.value = result.data;  // ✅ 来自数据库
        }
    } catch (error) {
        console.error('加载虚实比数据失败:', error);
    } finally {
        virtualRealRatioLoading.value = false;
    }
};
```

---

### 6. 期限结构页面 (term-structure)

#### 数据来源

| 数据项 | 函数 | API端点 | 数据库表 | 更新频率 | 状态 |
|-------|------|---------|---------|---------|------|
| **期限结构数据** | `loadTermStructure()` | `/api/v1/term-structure/{variety}` | `term_structure_data` | ✅ 每天15:30 | 正常 |
| **全品种结构** | `loadAllTermStructures()` | `/api/v1/term-structure/all` | `term_structure_data` | ✅ 每天15:30 | 正常 |
| **增强分析** | `loadTermEnhancedData()` | `/api/v1/term-structure/enhanced/{variety}` | `term_structure_enhanced` | ✅ 每天15:30 | 正常 |
| **结构变化** | `loadStructureChanges()` | `/api/v1/term-structure/structure-changes?lookback_days=7` | `term_structure_data` | ✅ 每天15:30 | 正常 |

---

## 🔍 问题诊断总结

### ❌ 确定的问题

#### 问题1: CSV文件数据过时

**位置**: 总览页面、基本面页面

**原因**:
- `期权分析_总览_V2.csv`
- `期权分析_多头Top5_V2.csv`
- `期权分析_空头Top5_V2.csv`

这3个CSV文件没有定时任务自动生成

**影响**:
- "研报"列数据可能过时
- "期限结构"列数据可能过时
- "综合方向"和"净得分"可能过时

**解决方案**:
1. 添加定时任务每天自动生成CSV
2. 或改为从数据库实时查询

---

#### 问题2: 持仓/席位数据"都一样"

**位置**: 持仓分析页面、席位分析页面

**现象**: 用户报告"切换不同席位出来的数据都一样"

**代码分析结果**:
- ✅ 前端参数传递正确
- ✅ API路由接收正确
- ✅ 爬虫使用参数正确

**可能原因**:
1. **交易可查网站问题**:
   - 网站返回缓存数据
   - Cookie权限问题导致某些品种返回默认数据
   - API限流导致返回上一次的数据

2. **前端缓存问题**:
   - 浏览器HTTP缓存
   - ECharts图表实例复用导致数据没更新

3. **数据解析问题**:
   - 某些品种的响应格式不同
   - 解析失败返回空数据或默认数据

**排查步骤**:
```bash
# 1. 查看后端日志
tail -f /opt/option-tracker/app.log | grep "正在获取"

# 2. 测试不同品种的API
curl "http://120.27.159.14/api/v1/capital/jiaoyikecha/螺纹钢/rb2603"
curl "http://120.27.159.14/api/v1/capital/jiaoyikecha/沪铜/cu2603"

# 3. 对比返回数据是否不同
```

**前端调试**:
```javascript
// 在前端console执行
console.log('当前品种:', positionVarietyName.value);
console.log('当前合约:', positionContractCode.value);
console.log('持仓数据:', JSON.stringify(positionData.value, null, 2));
console.log('完整数据:', JSON.stringify(positionFullData.value, null, 2));
```

**临时解决方案**:
```javascript
// 在loadPositionData函数中添加缓存破坏参数
const posUrl = `${API_BASE}/capital/jiaoyikecha/${encodeURIComponent(positionVarietyName.value)}/${positionContractCode.value}?_t=${Date.now()}`;
```

---

## 📊 数据更新频率汇总

| 数据源 | 更新方式 | 频率 | 状态 |
|--------|---------|------|------|
| 智汇期讯 | 定时爬虫 | 每30分钟 | ✅ 正常 |
| 方期看盘 | 定时爬虫 | 08:50/20:50 | ✅ 正常 |
| 交易可查蓝图 | 定时爬虫 | 19:00 | ✅ 正常 |
| 优矿席位 | 定时爬虫 | 16:00 | ✅ 正常 |
| Openvlab期权流 | 定时爬虫 | 交易时段每分钟 | ✅ 正常 |
| 虚实比 | 定时爬虫 | 18:00 | ✅ 正常 |
| 期限结构 | 定时爬虫 | 15:30 | ✅ 正常 |
| **CSV分析文件** | ❌ 手动 | ⚠️ 不定期 | **❌ 问题** |
| **实时席位数据** | 实时爬取 | 按需 | ⚠️ **可能有缓存** |

---

## 🔧 修复建议

### 优先级1: 修复CSV数据过时问题

**方案A**: 添加CSV生成定时任务
```python
# 在scheduler.py中添加
@scheduler.scheduled_job('cron', hour=19, minute=45)
def generate_analysis_csvs():
    """每天19:45生成分析CSV文件"""
    from app.services.analysis import AnalysisService
    db = SessionLocal()
    try:
        service = AnalysisService(db)
        service.generate_analysis_csvs()
    finally:
        db.close()
```

**方案B**: 改为数据库查询
```python
# 修改analysis_v2.py的路由
@router.get("/overview")
async def get_analysis_overview(db: Session = Depends(get_db)):
    # 不再读CSV，直接从数据库查询
    research_reports = db.query(ResearchReport).all()
    term_structures = db.query(TermStructureData).all()
    # 组合数据返回
    ...
```

### 优先级2: 排查席位数据"都一样"问题

1. **添加详细日志**
2. **测试不同品种API**
3. **添加缓存破坏参数**
4. **检查交易可查Cookie是否过期**

---

## 📝 完整API端点清单

### 基本面
- `GET /api/v1/fundamental/zhihui/market-sentiment` - 智汇期讯市场情绪
- `GET /api/v1/fundamental/zhihui/sentiment-stats` - 智汇期讯统计

### 资金面
- `GET /api/v1/capital/option-flow/all` - 期权资金流向汇总
- `GET /api/v1/capital/jiaoyikecha/{variety}/{contract}` - 实时席位数据
- `GET /api/v1/capital/jiaoyikecha/{variety}/{contract}/full` - 完整席位+趋势
- `GET /api/v1/capital/divergent-scan/latest` - 机构散户对立扫描
- `POST /api/v1/capital/seasonal-price` - 价格季节性

### 技术面
- `GET /api/v1/term-structure/{variety}` - 品种期限结构
- `GET /api/v1/term-structure/all` - 全品种期限结构
- `GET /api/v1/term-structure/enhanced/{variety}` - 增强分析
- `GET /api/v1/term-structure/structure-changes` - 结构变化

### 虚实比
- `GET /api/v1/option/virtual-real-ratio/all` - 全品种虚实比

### 分析
- `GET /api/v1/analysis/v2/overview` - 品种分析总览 ⚠️
- `GET /api/v1/analysis/v2/comprehensive-stats` - 综合统计 ⚠️
- `GET /api/v1/analysis/v2/opportunities` - 机会列表 ⚠️

---

生成于: 2026-01-12
系统版本: Option Tracker V2
