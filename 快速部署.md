# 🚀 快速部署指南

> 5分钟快速部署期权交易策略系统到外网

## 方法选择

根据你的情况选择最适合的部署方式:

```
有云服务器? → 使用 Docker 部署 (方法一)
没有服务器? → 使用 Railway/Render (方法二)
仅测试使用? → 使用 Render (最简单)
```

---

## 方法一: Docker部署 (有服务器)

### 1. 准备服务器

```bash
# SSH登录服务器
ssh user@your-server-ip

# 安装Docker (如未安装)
curl -fsSL https://get.docker.com | sh
```

### 2. 上传项目

```bash
# 方式1: Git克隆
git clone <your-repo>
cd option_tracker

# 方式2: 直接上传
scp -r ./option_tracker user@server:/path/
```

### 3. 配置并部署

```bash
# 配置环境变量
cp .env.production .env
nano .env  # 填入 UQER_TOKEN

# 一键部署
chmod +x deploy_docker.sh
./deploy_docker.sh
```

### 4. 外网访问配置

```bash
# 开放防火墙端口
sudo ufw allow 80
sudo ufw allow 443

# 云服务器记得在控制台开放安全组
```

### ✅ 完成!

访问: `http://your-server-ip/frontend`

---

## 方法二: Railway部署 (无服务器)

### 1. 推送到GitHub

```bash
cd option_tracker
git init
git add .
git commit -m "Deploy to Railway"
git remote add origin <your-github-repo>
git push -u origin main
```

### 2. 部署到Railway

1. 访问 https://railway.app
2. 点击 **"New Project"**
3. 选择 **"Deploy from GitHub repo"**
4. 选择你的仓库

### 3. 配置环境变量

在Railway项目设置中添加:

```
UQER_TOKEN=190bbd239ab55cb2f3f2919601622b0b793e94c35d7967e0c4b325682eddd981
DATABASE_URL=sqlite:///./option_tracker.db
```

### ✅ 完成!

Railway会自动分配域名: `https://option-tracker-production-xxxx.up.railway.app`

---

## 方法三: Render部署 (最简单)

### 1. 推送到GitHub (同方法二)

### 2. 在Render创建服务

1. 访问 https://render.com
2. 点击 **"New +"** → **"Web Service"**
3. 连接GitHub仓库
4. 配置:
   - Build Command: `pip install -r requirements.txt`
   - Start Command: `uvicorn main:app --host 0.0.0.0 --port $PORT`

### 3. 添加环境变量

```
UQER_TOKEN=your_token
DATABASE_URL=sqlite:///./option_tracker.db
PORT=8000
```

### ✅ 完成!

访问: `https://option-tracker.onrender.com`

---

## 🎯 访问系统

部署完成后,访问以下页面:

| 页面 | 路径 | 说明 |
|------|------|------|
| 导航首页 | `/` | 系统导航 |
| API文档 | `/docs` | Swagger文档 |
| 前端界面 | `/frontend` | 主界面 |
| 数据治理 | `/data-governance` | 数据质量监控 |
| V2分析 | `/analysis-v2` | 期权分析 |
| 机会雷达 | `/opportunity-radar` | 交易机会 |

---

## ⚡ 验证部署

```bash
# 检查健康状态
curl http://your-domain/health

# 应返回
{"status":"healthy"}
```

---

## 🔧 常见问题

### 1. 端口被占用?

```bash
# 修改 docker-compose.yml
ports:
  - "8080:8000"  # 改为其他端口
```

### 2. 无法外网访问?

**检查清单:**
- [ ] 防火墙已开放端口 80/443
- [ ] 云服务器安全组已配置
- [ ] 服务已正常启动

### 3. Railway/Render服务休眠?

Render免费版15分钟无访问会休眠,首次访问需等待启动。

**解决方案:**
- 使用 UptimeRobot 定期ping
- 或升级到付费版

---

## 📞 需要帮助?

- 查看完整文档: `部署完整指南.md`
- 查看日志: `docker-compose logs -f`
- 检查配置: 确保 `.env` 文件正确

---

## 🎉 下一步

系统部署成功后:

1. 访问 `/frontend` 查看主界面
2. 访问 `/docs` 查看API文档
3. 配置定时任务自动更新数据
4. (可选) 配置SSL证书启用HTTPS

**祝您使用愉快!**
