# ⚡ 每日早报 - 快速参考卡

**一页纸搞定所有操作** | 版本 v1.0 | 2025-12-28

---

## 🚀 3步启动

```bash
# 1. 进入项目目录
cd /Users/pm/Documents/期权交易策略/option_tracker

# 2. 启动服务器
uvicorn main:app --host 0.0.0.0 --port 8001

# 3. 打开浏览器
http://localhost:8001/frontend.html → 点击"🌅 每日早报"
```

---

## 🌐 访问地址

| 方式 | URL | 说明 |
|------|-----|------|
| **主系统** | http://localhost:8001/frontend.html | 点击"每日早报"Tab |
| **独立页面** | http://localhost:8001/daily_report.html | 直接访问早报 |
| **API文档** | http://localhost:8001/docs | Swagger UI |

---

## 📡 API速查

### 获取最新早报
```bash
curl http://localhost:8001/api/v1/daily-report/latest
```

### 手动生成早报
```bash
curl -X POST http://localhost:8001/api/v1/daily-report/generate
```

### 查询指定日期
```bash
curl "http://localhost:8001/api/v1/daily-report/by-date?date=2025-12-28"
```

### 获取历史列表
```bash
curl "http://localhost:8001/api/v1/daily-report/list?days=7"
```

### 预览（不保存）
```bash
curl http://localhost:8001/api/v1/daily-report/preview
```

---

## ⏰ 自动运行

- **时间**: 每天早上 08:30
- **状态**: ✅ 已配置
- **任务ID**: `daily_morning_report`
- **下次运行**: 2025-12-29 08:30

### 修改运行时间
编辑 `app/services/scheduler.py` 第49行:
```python
trigger=CronTrigger(hour=8, minute=30)  # 改成你想要的时间
```

---

## 📊 7大数据维度

| 维度 | 数据表 | 内容 |
|------|--------|------|
| 📈 市场资讯 | 金十数据API | 最新10条期货快讯 |
| 🎯 交易机会 | comprehensive_opportunities | S/A级机会 |
| 📋 研报观点 | zhihui_market_sentiment | 机构一致性 |
| 💰 资金流向 | option_capital_flow | 净流入/流出 |
| ⚠️ 风险预警 | virtual_real_ratio | 虚实比异常 |
| 📉 期限结构 | term_structure_enhanced | 结构转换 |
| 📊 技术信号 | daily_blueprints | 交易可查 |

---

## 🔍 快速诊断

### 问题1: 页面显示"暂无数据"
```bash
# 检查数据库
python3 -c "
from app.models.database import get_db
from sqlalchemy import text
db = next(get_db())
result = db.execute(text('SELECT COUNT(*) FROM daily_reports'))
print(f'早报记录数: {result.scalar()}')
"
```
**解决**: 点击"立即生成最新早报"按钮

---

### 问题2: 定时任务未执行
```bash
# 查看日志
tail -50 server.log | grep "早报"
```
**确认**: 应看到"每日早报定时任务已启动"

---

### 问题3: API调用失败
```bash
# 检查服务状态
curl http://localhost:8001/docs
```
**解决**: 确保服务器正在运行

---

## 📁 文件位置

### 后端代码
```
app/services/daily_report_generator.py  # 核心生成器
app/services/scheduler.py               # 定时任务
app/routers/daily_report.py            # API路由
```

### 前端代码
```
daily_report.html                        # 独立页面
frontend.html                            # 主系统（已集成Tab）
```

### 文档
```
每日早报使用指南.md                     # 📖 详细使用说明
每日早报_快速验证清单.md                # ✅ 功能验证
每日早报_前端界面说明.md                # 🎨 UI设计
每日早报功能实现报告.md                 # 🏗️ 技术架构
每日早报_完整文档索引.md                # 📚 文档导航
每日早报_完成确认书.md                  # 📝 交付确认
```

---

## 🎯 常用命令

### 安装依赖
```bash
pip install apscheduler httpx beautifulsoup4
```

### 手动生成（Python）
```python
import asyncio
from app.services.daily_report_generator import DailyReportGenerator

async def generate():
    gen = DailyReportGenerator()
    report = await gen.generate_daily_report()
    print(f"✅ 生成成功: {report['report_date']}")

asyncio.run(generate())
```

### 查看数据库
```bash
sqlite3 option_tracker.db "SELECT report_date, generate_time FROM daily_reports ORDER BY generate_time DESC LIMIT 5;"
```

### 清空早报记录（慎用）
```bash
sqlite3 option_tracker.db "DELETE FROM daily_reports;"
```

---

## 🎨 前端样式

### 主题色
```css
--gradient: linear-gradient(135deg, #667eea, #764ba2)
--bull-color: #ef4444   /* 红色 - 多头 */
--bear-color: #3b82f6   /* 蓝色 - 空头 */
--warning: #f59e0b      /* 橙色 - 警告 */
```

### 重要性标签
```
[高] 红色  #ef4444
[中] 橙色  #f59e0b
[低] 灰色  #6b7280
```

---

## 📖 文档速查

| 想要... | 看这个文档 | 章节 |
|---------|-----------|------|
| 快速上手 | 使用指南.md | 功能说明、访问方式 |
| 验证功能 | 快速验证清单.md | 功能验证 |
| 了解UI | 前端界面说明.md | 页面结构 |
| 修改代码 | 功能实现报告.md | 技术架构 |
| 找文档 | 完整文档索引.md | 文档导航 |

---

## ⚡ 性能指标

```
生成时间: < 10秒
API响应: < 1秒
页面加载: < 2秒
数据库查询: < 100ms
```

---

## 🔧 配置文件

### 定时任务
`app/services/scheduler.py` 第49行

### API路由
`main.py` 第68-69行

### 数据表
```sql
CREATE TABLE daily_reports (
  id INTEGER PRIMARY KEY,
  report_date VARCHAR(10),
  generate_time DATETIME,
  content TEXT,
  executive_summary TEXT,
  created_at DATETIME
);
```

---

## 📞 快速支持

### 查看完整文档
```bash
cat 每日早报_完整文档索引.md
```

### API交互测试
```bash
# 启动服务后访问
http://localhost:8001/docs
```

### 日志位置
```bash
tail -f server.log
```

---

## ✅ 验证清单

快速验证所有功能是否正常:

- [ ] 服务器启动成功
- [ ] 看到"每日早报定时任务已启动"日志
- [ ] 访问 frontend.html 正常
- [ ] "每日早报"Tab显示
- [ ] 点击Tab后内容加载
- [ ] API `/latest` 返回数据
- [ ] 手动生成按钮工作
- [ ] 数据库有记录

---

## 🎓 5分钟快速掌握

1. **阅读**: `每日早报使用指南.md` (5分钟)
2. **启动**: `uvicorn main:app` (1分钟)
3. **访问**: 打开浏览器查看 (1分钟)
4. **测试**: 点击"立即生成"按钮 (1分钟)
5. **验证**: 查看生成的早报 (2分钟)

**总计**: 10分钟完全掌握！

---

## 🚨 紧急联系

### 问题 = 解决方案

| 问题 | 一行命令解决 |
|------|-------------|
| 服务未启动 | `uvicorn main:app --port 8001` |
| 无法访问 | 检查 `http://localhost:8001/docs` |
| 数据为空 | `curl -X POST http://localhost:8001/api/v1/daily-report/generate` |
| 时间不对 | 编辑 `app/services/scheduler.py:49` |

---

**打印此页并贴在显示器旁 → 随时查阅！** 📋

---

**版本**: v1.0 | **日期**: 2025-12-28 | **状态**: ✅ Ready
