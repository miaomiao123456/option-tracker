# 🚀 你的专属部署指南

## Git 已配置完成 ✅
- GitHub 用户名: miaomiao123456
- 邮箱: 574876102@qq.com
- 远程仓库: https://github.com/miaomiao123456/option-tracker

---

## 📝 接下来的操作步骤

### 第一步：在 GitHub 创建仓库

1. **打开浏览器访问：**
   ```
   https://github.com/new
   ```

2. **填写仓库信息：**
   - Repository name: `option-tracker`
   - Description: `期权交易跟踪器 - 四维分析系统`
   - 选择: **Public** (公开)
   - ⚠️ **不要勾选** "Add a README file"
   - 点击: **Create repository**

---

### 第二步：推送代码到 GitHub

在终端执行这一条命令：

```bash
cd /Users/pm/Documents/期权交易策略/option_tracker && git push -u origin main
```

如果需要输入 GitHub 密码，使用 **Personal Access Token**（不是GitHub密码）

**获取 Token 的方法：**
1. 访问：https://github.com/settings/tokens/new
2. Note: `option-tracker-deploy`
3. Expiration: 90 days
4. 勾选: `repo` (所有权限)
5. 点击: Generate token
6. 复制 Token（以 ghp_ 开头）
7. 在命令行密码处粘贴 Token

---

### 第三步：在 Render.com 部署

1. **访问 Render.com：**
   ```
   https://render.com/
   ```

2. **注册/登录：**
   - 点击: **Get Started**
   - 选择: **Sign in with GitHub**
   - 授权 Render 访问你的 GitHub

3. **创建 Web Service：**
   - 点击: **New +** → **Web Service**
   - 找到: `miaomiao123456/option-tracker`
   - 点击: **Connect**

4. **配置服务（自动识别）：**

   系统会自动从 `render.yaml` 读取配置，你只需要：

   - Name: `option-alpha` (或任意名字)
   - 点击: **Advanced** 展开高级设置

5. **添加环境变量：**

   点击 **Add Environment Variable**，逐个添加：

   | Key | Value |
   |-----|-------|
   | `JYK_USER` | `18321399574` |
   | `JYK_PASS` | `yi2013405` |
   | `GEMINI_API_KEY` | `sk-IJhu2VBNt2G97XJeE6F82dD8047c4a2989326250068aA1F5` |

   其他变量已在 render.yaml 中配置，不需要重复添加。

6. **选择计划：**
   - 选择: **Free** (完全免费)
   - 点击: **Create Web Service**

7. **等待部署（5-10分钟）：**
   ```
   ✓ Building...
   ✓ Deploying...
   ✓ Service is live!
   ```

---

### 第四步：获取你的 API 地址

部署成功后，你会看到：

```
https://option-alpha.onrender.com
```

或类似的 URL（Render 自动分配）

---

### 第五步：修改前端配置

1. **打开文件：**
   ```
   /Users/pm/Documents/期权交易策略/option_tracker/frontend.html
   ```

2. **找到第 164 行，修改为：**
   ```javascript
   const API_BASE = 'https://你的应用名.onrender.com/api/v1';
   ```

   例如：
   ```javascript
   const API_BASE = 'https://option-alpha.onrender.com/api/v1';
   ```

3. **保存文件**

---

### 第六步：测试你的 API

在浏览器打开（替换成你的实际URL）：

1. **API文档：**
   ```
   https://option-alpha.onrender.com/docs
   ```

2. **健康检查：**
   ```
   https://option-alpha.onrender.com/health
   ```

3. **获取总览数据：**
   ```
   https://option-alpha.onrender.com/api/v1/summary/overview
   ```

---

### 第七步：部署前端到 Netlify（可选，免费）

1. **访问：**
   ```
   https://app.netlify.com/drop
   ```

2. **拖拽文件：**
   - 拖拽 `frontend.html` 到页面
   - 自动部署完成！

3. **获得前端地址：**
   ```
   https://random-name.netlify.app
   ```

---

## ⚠️ 重要提醒

### Render 免费版限制：
- ✅ 完全免费
- ⚠️ 15分钟无请求会休眠
- ⚠️ 首次唤醒需要 15-30 秒
- ✅ 每月 750 小时运行时间（够用）

### 解决休眠问题（可选）：
使用 UptimeRobot 免费服务定时 ping：
1. 访问：https://uptimerobot.com/
2. 添加监控：你的 Render URL
3. 间隔：5分钟
4. Render 会保持唤醒

---

## 🎊 完成后你将拥有：

- ✅ GitHub 代码托管（免费）
- ✅ Render.com API 服务（免费 + HTTPS）
- ✅ Netlify 前端托管（可选，免费）
- ✅ 全球 CDN 加速
- ✅ 自动持续部署（Git推送自动更新）

**总成本：$0/月**

---

## 🔧 后续更新代码

每次修改代码后：

```bash
cd /Users/pm/Documents/期权交易策略/option_tracker

git add .
git commit -m "更新功能"
git push

# Render 会自动检测并重新部署！
```

---

## 📞 需要帮助？

遇到问题可以查看：
1. [Render部署教程.md](Render部署教程.md) - 详细文档
2. Render Dashboard 的 Logs - 查看错误日志
3. https://render.com/docs - 官方文档

---

**现在开始第一步：**
访问 https://github.com/new 创建仓库吧！🚀
