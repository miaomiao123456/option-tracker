# 期权交易策略系统 - 完整部署指南

## 📋 目录

1. [部署方案概览](#部署方案概览)
2. [方案一: Docker部署(推荐)](#方案一-docker部署推荐)
3. [方案二: 云平台一键部署](#方案二-云平台一键部署)
4. [外网访问配置](#外网访问配置)
5. [常见问题](#常见问题)

---

## 部署方案概览

本系统支持多种部署方式:

| 方案 | 优点 | 缺点 | 适用场景 |
|------|------|------|---------|
| Docker | 环境一致、易于迁移 | 需要服务器 | 生产环境、自有服务器 |
| Railway | 免费、自动部署 | 有免费额度限制 | 快速测试、个人使用 |
| Render | 免费、稳定 | 休眠机制 | 演示、小规模使用 |

---

## 方案一: Docker部署(推荐)

### 前置要求

- 一台云服务器(阿里云/腾讯云/AWS等)
- 操作系统: Ubuntu 20.04+ / CentOS 7+
- 配置要求: 最低1核2G内存
- 已安装Docker和Docker Compose

### 1. 安装Docker

```bash
# Ubuntu/Debian
curl -fsSL https://get.docker.com | sh
sudo systemctl start docker
sudo systemctl enable docker

# 安装Docker Compose
sudo curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
sudo chmod +x /usr/local/bin/docker-compose
```

### 2. 上传项目文件

```bash
# 方式1: 使用git克隆
git clone <your-repo-url>
cd option_tracker

# 方式2: 使用scp上传
scp -r ./option_tracker user@your-server:/home/user/
```

### 3. 配置环境变量

```bash
cd option_tracker

# 复制环境配置文件
cp .env.production .env

# 编辑配置文件,填入必要信息
nano .env
```

**必填配置项:**
```env
UQER_TOKEN=your_uqer_token_here
```

### 4. 一键部署

```bash
# 执行部署脚本
chmod +x deploy_docker.sh
./deploy_docker.sh
```

### 5. 验证部署

```bash
# 检查服务状态
docker-compose ps

# 查看日志
docker-compose logs -f

# 测试访问
curl http://localhost:8000/health
```

### 6. 常用运维命令

```bash
# 停止服务
docker-compose down

# 重启服务
docker-compose restart

# 更新代码后重新部署
git pull
docker-compose up -d --build

# 查看实时日志
docker-compose logs -f option-tracker

# 备份数据库
cp data/option_tracker.db backup/option_tracker_$(date +%Y%m%d).db
```

---

## 方案二: 云平台一键部署

### 2.1 Railway部署

**优势:**
- 每月500小时免费额度
- 自动HTTPS
- 无需服务器

**步骤:**

1. **Fork项目到GitHub**
   ```bash
   # 将项目推送到你的GitHub仓库
   git init
   git add .
   git commit -m "Initial commit"
   git remote add origin <your-github-repo>
   git push -u origin main
   ```

2. **部署到Railway**
   - 访问 [Railway.app](https://railway.app)
   - 点击 "Start a New Project"
   - 选择 "Deploy from GitHub repo"
   - 选择你的仓库
   - Railway会自动检测并部署

3. **配置环境变量**
   - 在Railway项目设置中添加:
     ```
     UQER_TOKEN=your_token
     DATABASE_URL=sqlite:///./option_tracker.db
     ```

4. **获取访问地址**
   - Railway会自动分配域名
   - 格式: `https://option-tracker-production-xxxx.up.railway.app`

### 2.2 Render部署

**优势:**
- 完全免费
- 自动SSL证书

**步骤:**

1. **推送到GitHub** (同上)

2. **在Render创建Web Service**
   - 访问 [Render.com](https://render.com)
   - 点击 "New +" → "Web Service"
   - 连接GitHub仓库
   - 配置:
     - **Environment:** Python 3
     - **Build Command:** `pip install -r requirements.txt`
     - **Start Command:** `uvicorn main:app --host 0.0.0.0 --port $PORT`

3. **添加环境变量**
   ```
   UQER_TOKEN=your_token
   DATABASE_URL=sqlite:///./option_tracker.db
   PORT=8000
   ```

4. **部署完成**
   - 域名格式: `https://option-tracker.onrender.com`

---

## 外网访问配置

### 方式1: 使用Nginx反向代理(推荐)

系统已包含nginx配置,默认在docker-compose中启用。

**配置域名访问:**

1. **配置DNS解析**
   ```
   A记录: yourdomain.com → 服务器IP
   ```

2. **修改nginx.conf**
   ```nginx
   server_name yourdomain.com;
   ```

3. **配置SSL证书(使用Let's Encrypt)**
   ```bash
   # 安装certbot
   sudo apt install certbot python3-certbot-nginx

   # 获取证书
   sudo certbot --nginx -d yourdomain.com
   ```

4. **重启服务**
   ```bash
   docker-compose restart nginx
   ```

### 方式2: 配置防火墙

**开放端口:**

```bash
# Ubuntu (ufw)
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp
sudo ufw enable

# CentOS (firewalld)
sudo firewall-cmd --permanent --add-port=80/tcp
sudo firewall-cmd --permanent --add-port=443/tcp
sudo firewall-cmd --reload
```

**云服务器安全组:**
- 在云服务商控制台开放端口 80, 443

### 方式3: 使用frp内网穿透

如果没有公网IP,可以使用frp:

```bash
# 下载frp客户端
wget https://github.com/fatedier/frp/releases/download/v0.52.0/frp_0.52.0_linux_amd64.tar.gz
tar -xzf frp_0.52.0_linux_amd64.tar.gz
cd frp_0.52.0_linux_amd64

# 配置frpc.ini
[common]
server_addr = frp服务器地址
server_port = 7000

[option-tracker]
type = http
local_port = 8000
custom_domains = yourdomain.com

# 启动客户端
./frpc -c frpc.ini
```

---

## 访问地址

部署完成后,可以通过以下地址访问:

- **首页导航:** `http://your-domain/`
- **API文档:** `http://your-domain/docs`
- **前端界面:** `http://your-domain/frontend`
- **健康检查:** `http://your-domain/health`

### 功能页面:

- 数据治理: `/data-governance`
- V2分析: `/analysis-v2`
- 智汇期讯: `/zhihui`
- 虚实比分析: `/virtual_real_ratio.html`
- 机会雷达: `/opportunity-radar`
- 资金流向: `/capital_flow.html`

---

## 常见问题

### Q1: Docker镜像构建失败?

**解决方案:**
```bash
# 清理Docker缓存
docker system prune -a

# 重新构建
docker-compose build --no-cache
```

### Q2: 端口被占用?

**解决方案:**
```bash
# 查看占用端口的进程
sudo lsof -i :8000

# 修改docker-compose.yml中的端口映射
ports:
  - "8080:8000"  # 改为8080
```

### Q3: 数据库文件权限问题?

**解决方案:**
```bash
# 修改目录权限
sudo chown -R 1000:1000 data/
sudo chmod -R 755 data/
```

### Q4: 服务无法外网访问?

**检查清单:**
- [ ] 防火墙是否开放端口
- [ ] 云服务器安全组是否配置
- [ ] nginx配置是否正确
- [ ] DNS解析是否生效

**调试命令:**
```bash
# 检查端口监听
netstat -tlnp | grep 8000

# 测试本地访问
curl http://localhost:8000/health

# 测试外网访问
curl http://your-server-ip:8000/health
```

### Q5: Railway/Render服务休眠?

**Render免费版会在15分钟无访问后休眠。**

**解决方案:**
- 使用UptimeRobot等服务定期ping
- 升级到付费版本

### Q6: Playwright浏览器安装失败?

**云平台可能不支持Playwright。**

**解决方案:**
- 当前系统已切换到优矿SDK(不需要浏览器)
- 如需Playwright,需使用Docker部署

---

## 监控与维护

### 日志查看

```bash
# Docker部署
docker-compose logs -f

# 查看特定服务日志
docker-compose logs -f option-tracker

# 查看最近100行
docker-compose logs --tail=100
```

### 性能监控

```bash
# 查看容器资源使用
docker stats

# 查看磁盘使用
df -h

# 查看数据库大小
du -h data/option_tracker.db
```

### 数据备份

```bash
# 手动备份脚本
#!/bin/bash
DATE=$(date +%Y%m%d_%H%M%S)
BACKUP_DIR="backups"
mkdir -p $BACKUP_DIR

# 备份数据库
cp data/option_tracker.db $BACKUP_DIR/option_tracker_$DATE.db

# 备份配置
cp .env $BACKUP_DIR/.env_$DATE

# 压缩
tar -czf $BACKUP_DIR/backup_$DATE.tar.gz $BACKUP_DIR/*_$DATE*

echo "备份完成: $BACKUP_DIR/backup_$DATE.tar.gz"
```

---

## 技术支持

如遇到问题,请:

1. 查看日志文件
2. 检查环境配置
3. 参考本文档常见问题章节
4. 提交Issue到GitHub仓库

---

## 更新日志

- 2024-12-25: 初始版本,支持Docker和云平台部署
